# Core database requirements - pinned to avoid compatibility issues
sqlalchemy>=1.4.0,<2.0.0

# Web API requirements (optional - install if you want to use the Flask API)
flask>=2.0.0,<3.0.0
flask-cors>=3.0.0,<5.0.0

# Data processing and export
pandas>=1.3.0,<2.0.0

# Date/time utilities
python-dateutil>=2.8.0

# Development and testing (optional)
pytest>=6.0.0
pytest-cov>=2.0.0

# JSON handling (usually built-in, but just in case)
# json - built into Python standard library

# CSV handling (built-in to Python)
# csv - built into Python standard library

# For command line interface
argparse  # built into Python standard library

# SQLite support (usually built-in with Python)
# sqlite3 - built into Python standard library

# Optional: For better database performance with larger datasets
# psycopg2-binary>=2.9.0  # PostgreSQL support
# pymysql>=1.0.0          # MySQL support

# Optional: For advanced data analysis
# numpy>=1.20.0
# matplotlib>=3.3.0
# seaborn>=0.11.0

# Optional: For Excel export functionality
# openpyxl>=3.0.0
# xlsxwriter>=1.4.0